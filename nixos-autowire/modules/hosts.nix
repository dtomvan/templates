{
  hosts = {
    # `test1` doesn't mean anything here. Just used as an ID that doesn't change. hostName can change though.
    test1 = {
      hostName = "nixos";
      system = "x86_64-linux";
    };
  };
}
